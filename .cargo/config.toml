[build]
jobs = -1

[env]
RUST_BACKTRACE = "1"
RUSTBERT_CACHE = { value = ".cache", relative = true }
# RUST_TEST_NOCAPTURE = "1"

[net]
retry = 3

[alias]
lint = "clippy --workspace --all-features -- -D warnings"
